# Асинхронные операции

## Задача 3.1: Параллельная загрузка данных 🟢

**Контекст:** Нужно загрузить данные пользователя, его посты и комментарии одновременно.

**Задача:**
```typescript
async function fetchUserData(userId: string) {
  // Загрузить параллельно:
  // - /api/users/:id
  // - /api/users/:id/posts
  // - /api/users/:id/comments

  // Вернуть объект со всеми данными
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте `Promise.all()`
- Обработайте случай, когда один из запросов упал
- Подумайте о `Promise.allSettled()` если нужны частичные данные

</details>
---

## Задача 3.2: Retry логика для failed запросов 🟡

**Контекст:** Если запрос упал с сетевой ошибкой, повторить его до 3 раз.

**Задача:**
```typescript
async function fetchWithRetry<T>(
  url: string,
  options: RequestInit = {},
  maxRetries: number = 3
): Promise<T> {
  // Реализовать retry логику
  // При каждой неудаче ждать перед следующей попыткой (1s, 2s, 4s - exponential backoff)
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте цикл или рекурсию
- Добавьте задержку между попытками (`await sleep(delay)`)
- Увеличивайте задержку экспоненциально: delay = delay * 2
- Пробрасывайте ошибку если все попытки исчерпаны

</details>
---

## Задача 3.3: Кэширование API запросов 🟡

**Контекст:** Избежать повторных запросов к API для одних и тех же данных.

**Задача:**
```typescript
class APICache {
  cache: Map<string, { data: any; timestamp: number }>;
  ttl: number; // time to live в миллисекундах

  async fetch<T>(url: string, fetcher: () => Promise<T>): Promise<T> {
    // Если данные в кэше и не истекли - вернуть из кэша
    // Иначе - выполнить fetcher, сохранить в кэш, вернуть
  }

  clear(): void {}
}

// Использование:
const cache = new APICache(60000); // 1 минута
const data = await cache.fetch('/api/users', () => fetch('/api/users').then(r => r.json()));
```

<details>
<summary>💡 Подсказки</summary>

- Используйте URL как ключ кэша
- Сохраняйте timestamp при кэшировании
- Проверяйте актуальность: `Date.now() - timestamp < ttl`
- Предусмотрите метод для очистки кэша

</details>
---

## Задача 3.4: Загрузка с пагинацией (бесконечный скролл) 🟡

**Контекст:** Загружать следующую страницу данных при прокрутке до конца списка.

**Задача:**
```typescript
class InfiniteScroll {
  page: number;
  loading: boolean;
  hasMore: boolean;
  items: any[];

  async loadMore(): Promise<void> {
    // Загрузить следующую страницу
    // Добавить к существующим items
    // Обновить hasMore
  }

  reset(): void {
    // Сбросить к первой странице
  }
}

// API: GET /api/items?page=1&limit=20
```

<details>
<summary>💡 Подсказки</summary>

- Храните номер текущей страницы
- Флаг loading предотвращает двойные запросы
- hasMore = false если пришло меньше элементов чем limit
- Используйте Intersection Observer API для определения прокрутки до конца

</details>
---

## Задача 3.5: Отмена запросов при размонтировании 🟡

**Контекст:** Отменять fetch запросы когда компонент размонтируется (чтобы избежать обновления state).

**Задача:**
```typescript
// Использовать AbortController для отмены запросов

class CancellableRequest {
  controller: AbortController;

  async fetch<T>(url: string): Promise<T> {
    // Использовать this.controller.signal в fetch
  }

  cancel(): void {
    // Отменить текущий запрос
  }
}

// В React:
useEffect(() => {
  const request = new CancellableRequest();
  request.fetch('/api/data').then(setData);

  return () => request.cancel(); // cleanup
}, []);
```

<details>
<summary>💡 Подсказки</summary>

- Создайте новый AbortController
- Передайте signal в fetch options
- В cancel() вызовите controller.abort()
- Обработайте AbortError отдельно от других ошибок

</details>
---

## Задача 3.6: Последовательная загрузка зависимых данных 🟢

**Контекст:** Сначала нужно загрузить пользователя, затем на основе его данных загрузить другое.

**Задача:**
```typescript
async function fetchUserWithDetails(userId: string) {
  // 1. Загрузить пользователя: GET /api/users/:id
  // 2. Из ответа взять companyId
  // 3. Загрузить компанию: GET /api/companies/:companyId
  // 4. Вернуть объект { user, company }
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте await последовательно
- Обработайте ошибки на каждом шаге
- Можно использовать async/await или цепочку .then()

</details>
---

## Задача 3.7: Race condition при быстром переключении 🔴

**Контекст:** Пользователь быстро переключает табы, старые запросы могут прийти позже новых.

**Задача:**
```typescript
// Проблема:
// 1. Выбран tab A, запрос начат
// 2. Выбран tab B, запрос начат
// 3. Ответ для B пришел, UI обновлен
// 4. Ответ для A пришел (позже!), UI перезаписан старыми данными

// Решение: отслеживать последний запрос

class LatestRequestTracker {
  latestRequestId: number;

  async fetch<T>(fetcher: () => Promise<T>): Promise<T | null> {
    // Генерировать уникальный requestId
    // Выполнить fetcher
    // Если к моменту ответа есть более новый запрос - игнорировать результат
  }
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте счетчик для уникального requestId
- Сохраняйте requestId перед запросом
- После получения ответа проверяйте: если requestId !== latestRequestId, игнорируйте
- Альтернатива: отменять предыдущий запрос через AbortController

</details>
---

## Задача 3.8: Batch запросы 🟡

**Контекст:** Вместо 10 отдельных запросов для товаров, объединить в один batch запрос.

**Задача:**
```typescript
class BatchRequestQueue {
  queue: string[];
  timeout: NodeJS.Timeout | null;
  batchDelay: number; // 50ms

  async fetchItem(id: string): Promise<any> {
    // Добавить id в очередь
    // Запустить таймер (если еще не запущен)
    // Через batchDelay отправить batch запрос для всех id в queue
    // Вернуть результат для конкретного id
  }

  private async executeBatch(): Promise<void> {
    // POST /api/items/batch { ids: [...] }
    // Распределить результаты по промисам
  }
}

// Использование:
const batcher = new BatchRequestQueue();
const item1 = await batcher.fetchItem('1');
const item2 = await batcher.fetchItem('2'); // будут в одном запросе если вызваны близко по времени
```

<details>
<summary>💡 Подсказки</summary>

- Используйте setTimeout для задержки перед отправкой batch
- Храните промисы для каждого id
- После batch запроса resolve промисы с соответствующими данными
- Очищайте queue после отправки

</details>
---

## Задача 3.9: Polling с остановкой 🟡

**Контекst:** Опрашивать API каждые 5 секунд пока задача не выполнится.

**Задача:**
```typescript
interface Task {
  id: string;
  status: 'pending' | 'processing' | 'completed' | 'failed';
  result?: any;
}

async function pollTaskStatus(
  taskId: string,
  interval: number = 5000,
  maxAttempts: number = 60
): Promise<Task> {
  // Опрашивать GET /api/tasks/:id каждые interval мс
  // Остановиться когда status = 'completed' или 'failed'
  // Или после maxAttempts попыток
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте setInterval или рекурсивный setTimeout
- Проверяйте статус после каждого запроса
- Resolve промис при завершении
- Reject если превышено maxAttempts или статус 'failed'
- setTimeout предпочтительнее setInterval (не накапливаются запросы)

</details>
---

## Задача 3.10: Загрузка файла с прогресс-баром 🟡

**Контекст:** При загрузке файла показывать прогресс (0-100%).

**Задача:**
```typescript
async function uploadFileWithProgress(
  file: File,
  onProgress: (progress: number) => void
): Promise<Response> {
  // Загрузить файл с отслеживанием прогресса
  // Вызывать onProgress(percentage) по мере загрузки
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте XMLHttpRequest вместо fetch (fetch не поддерживает upload progress)
- Или используйте fetch с ReadableStream для скачивания
- Слушайте событие progress на xhr.upload
- Вычисляйте процент: (loaded / total) * 100

</details>
---

## Задача 3.11: Очередь последовательных запросов 🟡

**Контекст:** Несколько операций должны выполняться строго последовательно (например, обновления).

**Задача:**
```typescript
class RequestQueue {
  queue: Array<() => Promise<any>>;
  running: boolean;

  async add<T>(request: () => Promise<T>): Promise<T> {
    // Добавить запрос в очередь
    // Запустить обработку если еще не запущена
    // Вернуть результат когда очередь дойдет до этого запроса
  }

  private async process(): Promise<void> {
    // Выполнять запросы из queue по одному
  }
}

// Использование:
const queue = new RequestQueue();
queue.add(() => updateUser(1, { name: 'New Name' }));
queue.add(() => updateUser(1, { email: 'new@example.com' }));
// Запросы выполнятся последовательно
```

<details>
<summary>💡 Подсказки</summary>

- Храните очередь функций, возвращающих Promise
- Флаг running предотвращает параллельную обработку
- Используйте await для ожидания каждого запроса
- После выполнения всех запросов устанавливайте running = false

</details>
---

## Задача 3.12: Timeout для fetch запросов 🟢

**Контекст:** Если запрос не завершился за 10 секунд, отменить его.

**Задача:**
```typescript
async function fetchWithTimeout<T>(
  url: string,
  timeout: number = 10000
): Promise<T> {
  // Выполнить fetch с таймаутом
  // Если не успел за timeout - отменить и выбросить ошибку
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте AbortController
- Создайте Promise.race между fetch и setTimeout
- В setTimeout вызовите controller.abort()
- Выбросьте понятную ошибку TimeoutError

</details>