# Работа со строками и форматирование

## Задача 6.1: Форматирование валюты 🟢

**Контекст:** Отображать цены в правильном формате с валютой.

**Задача:**
```typescript
function formatCurrency(
  amount: number,
  currency: string = 'USD',
  locale: string = 'en-US'
): string {
  // 1000 → '$1,000.00'
  // 1234.56 → '$1,234.56'
  // 0.5 → '$0.50'
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте Intl.NumberFormat API
- Параметры: style: 'currency', currency
- Учитывайте локаль для разделителей (запятая/точка)

</details>


## Задача 6.2: Форматирование дат 🟢

**Контекст:** Отображать даты в разных форматах в зависимости от контекста.

**Задача:**
```typescript
function formatDate(date: Date | string, format: 'short' | 'long' | 'relative'): string {
  // short: '12/31/2024'
  // long: 'December 31, 2024'
  // relative: '2 hours ago', 'yesterday', '3 days ago'
}
```

<details>
<summary>💡 Подсказки</summary>

- Для short/long используйте Intl.DateTimeFormat
- Для relative вычисляйте разницу между датами
- Обрабатывайте: секунды, минуты, часы, дни, недели, месяцы
- Учитывайте единственное/множественное число

</details>


## Задача 6.3: Склонение числительных 🟡

**Контекст:** Правильно склонять слова с числами (для русского и других языков).

**Задача:**
```typescript
function pluralize(count: number, words: [string, string, string]): string {
  // Для русского: ['товар', 'товара', 'товаров']
  // 1 товар
  // 2 товара
  // 5 товаров
  // 21 товар
  // 22 товара
}

// Использование:
pluralize(1, ['item', 'items', 'items']); // '1 item'
pluralize(5, ['item', 'items', 'items']); // '5 items'
```

<details>
<summary>💡 Подсказки</summary>

- Для русского: остаток от деления на 10 и 100
- 1, 21, 31... → форма 1
- 2-4, 22-24... → форма 2
- 5-20, 25-30... → форма 3
- Для английского проще: count === 1 ? singular : plural
- Используйте Intl.PluralRules для универсального решения

</details>


## Задача 6.4: Обрезка текста с многоточием 🟢

**Контекст:** Ограничить длину текста для превью, добавив "..." в конце.

**Задача:**
```typescript
function truncate(text: string, maxLength: number, suffix: string = '...'): string {
  // 'This is a long text' → 'This is a...' (maxLength = 10)
  // Не обрезать посередине слова
  // Учитывать длину suffix
}
```

<details>
<summary>💡 Подсказки</summary>

- Если text.length <= maxLength, вернуть как есть
- Обрезайте до maxLength - suffix.length
- Найдите последний пробел в обрезанной строке
- Обрежьте до него чтобы не разрывать слово

</details>


## Задача 6.5: Генерация slug из заголовка 🟢

**Контекст:** Создать URL-friendly строку из заголовка статьи.

**Задача:**
```typescript
function slugify(text: string): string {
  // 'Hello World!' → 'hello-world'
  // 'JavaScript: The Good Parts' → 'javascript-the-good-parts'
  // 'Привет Мир' → 'privet-mir' (транслитерация)
}
```

<details>
<summary>💡 Подсказки</summary>

- Привести к нижнему регистру
- Заменить пробелы на дефисы
- Удалить спецсимволы (оставить буквы, цифры, дефисы)
- Удалить повторяющиеся дефисы
- Убрать дефисы в начале/конце
- Для транслитерации: создать mapping кириллица → латиница

</details>


## Задача 6.6: Парсинг URL параметров 🟢

**Контекст:** Извлечь query параметры из URL.

**Задача:**
```typescript
function parseQueryString(url: string): Record<string, string | string[]> {
  // '?name=John&age=30' → { name: 'John', age: '30' }
  // '?tags=js&tags=ts' → { tags: ['js', 'ts'] }
  // '?search=hello%20world' → { search: 'hello world' } (декодировать)
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте URLSearchParams API (самый простой способ)
- Или парсите вручную: split по '&', затем по '='
- Используйте decodeURIComponent для декодирования
- Если параметр повторяется - собрать в массив

</details>


## Задача 6.7: Подсветка поискового запроса в тексте 🟡

**Контекст:** Выделить найденные слова в результатах поиска.

**Задача:**
```typescript
function highlightText(text: string, query: string): string {
  // text: 'JavaScript is awesome'
  // query: 'script'
  // → 'Java<mark>Script</mark> is awesome'

  // Поиск case-insensitive
  // Сохранить оригинальный регистр
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте регулярное выражение с флагом 'gi'
- replace() с callback для сохранения оригинального регистра
- Экранируйте спецсимволы в query перед использованием в regex
- Оберните найденное в <mark> или <span class="highlight">

</details>


## Задача 6.8: Валидация и форматирование номера карты 🟡

**Контекст:** Форматировать номер карты с пробелами при вводе.

**Задача:**
```typescript
function formatCardNumber(input: string): string {
  // '1234567890123456' → '1234 5678 9012 3456'
  // '1234 5678' → '1234 5678'
  // Удалять нечисловые символы
  // Ограничить 16 цифрами
}

function detectCardType(cardNumber: string): 'visa' | 'mastercard' | 'amex' | 'unknown' {
  // Определить тип карты по первым цифрам
  // Visa: начинается с 4
  // MasterCard: 51-55 или 2221-2720
  // Amex: 34 или 37
}
```

<details>
<summary>💡 Подсказки</summary>

- Удалите все нечисловые символы
- Разделите на группы по 4 цифры (join с пробелом)
- Для Amex формат другой: 4-6-5
- Используйте регулярные выражения для определения типа

</details>


## Задача 6.9: Escape HTML 🟢

**Контекст:** Безопасно вставить пользовательский текст в HTML.

**Задача:**
```typescript
function escapeHtml(text: string): string {
  // '<script>alert("xss")</script>' → '&lt;script&gt;alert(&quot;xss&quot;)&lt;/script&gt;'
  // Экранировать: < > & " '
}

function unescapeHtml(text: string): string {
  // Обратное преобразование
}
```

<details>
<summary>💡 Подсказки</summary>

- Создайте mapping специальных символов
- Используйте replace с regex или несколько replace
- Или создайте временный div и используйте textContent/innerHTML

</details>


## Задача 6.10: Извлечение хэштегов и упоминаний 🟡

**Контекст:** Парсить текст поста для поиска #хэштегов и @упоминаний.

**Задача:**
```typescript
function extractHashtags(text: string): string[] {
  // 'Hello #world and #javascript!' → ['world', 'javascript']
}

function extractMentions(text: string): string[] {
  // 'Hello @john and @jane_doe!' → ['john', 'jane_doe']
}

function linkifyText(text: string): string {
  // 'Hello @john #world' →
  // 'Hello <a href="/users/john">@john</a> <a href="/tags/world">#world</a>'
}
```

<details>
<summary>💡 Подсказки</summary>

- Используйте regex: /#(\w+)/g для хэштегов
- /@(\w+)/g для упоминаний
- matchAll() или match() для извлечения
- replace() с callback для замены на ссылки

</details>


## Задача 6.11: Нормализация пробелов 🟢

**Контекст:** Очистить текст от лишних пробелов.

**Задача:**
```typescript
function normalizeWhitespace(text: string): string {
  // '  Hello    world  ' → 'Hello world'
  // Убрать пробелы в начале/конце
  // Заменить множественные пробелы на один
  // Заменить \n\n\n на \n\n (максимум два переноса подряд)
}
```

<details>
<summary>💡 Подсказки</summary>

- trim() для начала/конца
- replace(/\s+/g, ' ') для множественных пробелов
- replace(/\n{3,}/g, '\n\n') для переносов

</details>


## Задача 6.12: Генерация initials из имени 🟢

**Контекст:** Создать инициалы для аватара.

**Задача:**
```typescript
function getInitials(name: string, maxChars: number = 2): string {
  // 'John Doe' → 'JD'
  // 'John' → 'J'
  // 'Jean-Claude Van Damme' → 'JV' (maxChars = 2)
  // 'Jean-Claude Van Damme' → 'JVD' (maxChars = 3)
}
```

<details>
<summary>💡 Подсказки</summary>

- Разделите по пробелам
- Возьмите первую букву каждого слова
- Ограничьте maxChars символами
- Приведите к верхнему регистру
- Обработайте пустую строку

</details>


## Задача 6.13: Маскирование чувствительных данных 🟢

**Контекст:** Скрыть часть email, телефона, номера карты.

**Задача:**
```typescript
function maskEmail(email: string): string {
  // 'john.doe@example.com' → 'j***e@example.com'
  // Показать первую и последнюю букву до @
}

function maskPhone(phone: string): string {
  // '+1234567890' → '+1234***890'
  // Показать код страны и последние 3 цифры
}

function maskCard(cardNumber: string): string {
  // '1234567890123456' → '1234 **** **** 3456'
}
```

<details>
<summary>💡 Подсказки</summary>

- Разделите строку на части
- Замените средние символы на *
- Для email: split по @
- Сохраните структуру (пробелы, дефисы)

</details>


## Задача 6.14: Определение языка текста 🔴

**Контекст:** Попытаться определить язык введенного текста.

**Задача:**
```typescript
function detectLanguage(text: string): 'en' | 'ru' | 'unknown' {
  // Базовое определение по диапазонам символов
  // 'Hello world' → 'en'
  // 'Привет мир' → 'ru'
}
```

<details>
<summary>💡 Подсказки</summary>

- Проверьте наличие кириллических символов: /[\u0400-\u04FF]/
- Латинские: /[a-zA-Z]/
- Посчитайте процент символов каждого алфавита
- Более точно: используйте библиотеки или ML модели

</details>