# Работа с массивами и объектами

## Задача 1.1: Трансформация данных пользователей из API 🟢

**Контекст:** Вы получили данные пользователей от бэкенда и нужно преобразовать их в формат, удобный для фронтенда.

**Задача:**
```typescript
// Входные данные от API
const apiUsers = [
  { id: 1, first_name: 'John', last_name: 'Doe', email: 'john@example.com', is_active: true },
  { id: 2, first_name: 'Jane', last_name: 'Smith', email: 'jane@example.com', is_active: false },
];

// Нужно получить:
// [
//   { id: 1, fullName: 'John Doe', email: 'john@example.com', active: true },
//   { id: 2, fullName: 'Jane Smith', email: 'jane@example.com', active: false },
// ]
```

**Подсказки:**
- Используйте `map()` для трансформации
- Создайте функцию `transformUser()` для переиспользования
- Типизируйте входные и выходные данные в TypeScript

---

## Задача 1.2: Группировка товаров по категориям 🟢

**Контекст:** У вас есть список товаров, нужно сгруппировать их по категориям для отображения в UI.

**Задача:**
```typescript
const products = [
  { id: 1, name: 'Laptop', category: 'Electronics', price: 1000 },
  { id: 2, name: 'Shirt', category: 'Clothing', price: 50 },
  { id: 3, name: 'Phone', category: 'Electronics', price: 800 },
  { id: 4, name: 'Jeans', category: 'Clothing', price: 80 },
];

// Нужно получить:
// {
//   Electronics: [{ id: 1, name: 'Laptop', price: 1000 }, { id: 3, name: 'Phone', price: 800 }],
//   Clothing: [{ id: 2, name: 'Shirt', price: 50 }, { id: 4, name: 'Jeans', price: 80 }]
// }
```

**Подсказки:**
- Используйте `reduce()` для накопления результата
- Проверяйте существование категории перед добавлением
- Можно создать generic функцию `groupBy(array, key)`

---

## Задача 1.3: Удаление дубликатов из массива объектов 🟡

**Контекст:** После слияния данных из разных источников появились дубликаты пользователей по email.

**Задача:**
```typescript
const users = [
  { id: 1, email: 'john@example.com', name: 'John' },
  { id: 2, email: 'jane@example.com', name: 'Jane' },
  { id: 3, email: 'john@example.com', name: 'Johnny' }, // дубликат
  { id: 4, email: 'bob@example.com', name: 'Bob' },
];

// Оставить уникальные по email (первое вхождение)
```

**Подсказки:**
- `Set` не работает напрямую с объектами
- Используйте `Map` или отслеживайте уже встреченные email
- Решите, какую версию дубликата оставлять (первую или последнюю)

---

## Задача 1.4: Фильтрация и сортировка списка товаров 🟢

**Контекст:** Пользователь применил фильтры и сортировку в каталоге товаров.

**Задача:**
```typescript
const products = [
  { id: 1, name: 'Laptop Pro', category: 'Electronics', price: 1500, inStock: true },
  { id: 2, name: 'Laptop Air', category: 'Electronics', price: 1200, inStock: false },
  { id: 3, name: 'Mouse', category: 'Accessories', price: 50, inStock: true },
  { id: 4, name: 'Keyboard', category: 'Accessories', price: 100, inStock: true },
];

// Создать функцию: filterAndSort(products, filters, sortBy)
// filters = { category: 'Electronics', inStock: true, minPrice: 1000 }
// sortBy = 'price' | 'name' (ascending)
```

**Подсказки:**
- Разделите фильтрацию и сортировку
- Используйте `filter()` с несколькими условиями
- `sort()` мутирует массив, создайте копию
- Сделайте функцию гибкой для разных типов сортировки

---

## Задача 1.5: Нормализация вложенных данных 🟡

**Контекст:** API вернул вложенную структуру постов с комментариями, нужно нормализовать для state management.

**Задача:**
```typescript
const posts = [
  {
    id: 1,
    title: 'Post 1',
    author: { id: 101, name: 'John' },
    comments: [
      { id: 201, text: 'Great!', author: { id: 102, name: 'Jane' } },
      { id: 202, text: 'Nice', author: { id: 103, name: 'Bob' } },
    ]
  },
  {
    id: 2,
    title: 'Post 2',
    author: { id: 102, name: 'Jane' },
    comments: [
      { id: 203, text: 'Cool', author: { id: 101, name: 'John' } },
    ]
  }
];

// Нужно получить:
// {
//   posts: { 1: { id: 1, title: 'Post 1', authorId: 101, commentIds: [201, 202] }, ... },
//   comments: { 201: { id: 201, text: 'Great!', authorId: 102 }, ... },
//   users: { 101: { id: 101, name: 'John' }, ... }
// }
```

**Подсказки:**
- Пройдитесь по массиву и извлекайте каждую сущность отдельно
- Используйте объекты как словари (id → объект)
- Замените вложенные объекты на ID ссылки

---

## Задача 1.6: Поиск по массиву объектов 🟢

**Контекст:** Реализовать поиск по имени и email в списке пользователей (как в поисковой строке).

**Задача:**
```typescript
const users = [
  { id: 1, name: 'John Doe', email: 'john@example.com' },
  { id: 2, name: 'Jane Smith', email: 'jane@example.com' },
  { id: 3, name: 'Bob Johnson', email: 'bob@company.com' },
];

// Функция: searchUsers(users, query)
// query = 'john' должен найти John Doe и Bob Johnson
```

**Подсказки:**
- Поиск должен быть case-insensitive
- Проверяйте несколько полей (name, email)
- Используйте `includes()` или регулярные выражения

---

## Задача 1.7: Слияние данных из разных источников 🟡

**Контекст:** У вас есть базовая информация о пользователях и дополнительные данные из другого API. Нужно объединить.

**Задача:**
```typescript
const baseUsers = [
  { id: 1, name: 'John', email: 'john@example.com' },
  { id: 2, name: 'Jane', email: 'jane@example.com' },
];

const additionalData = [
  { userId: 1, avatar: 'avatar1.jpg', role: 'admin' },
  { userId: 2, avatar: 'avatar2.jpg', role: 'user' },
  { userId: 3, avatar: 'avatar3.jpg', role: 'user' }, // нет в baseUsers
];

// Объединить по id/userId, добавить поля из additionalData
```

**Подсказки:**
- Создайте Map из одного массива для быстрого доступа по id
- Решите, что делать с записями, которые есть только в одном источнике
- Используйте spread operator для слияния объектов

---

## Задача 1.8: Вычисление статистики по массиву 🟢

**Контекст:** На странице заказов нужно показать статистику: общая сумма, средний чек, количество.

**Задача:**
```typescript
const orders = [
  { id: 1, total: 100, status: 'completed' },
  { id: 2, total: 250, status: 'completed' },
  { id: 3, total: 150, status: 'pending' },
  { id: 4, total: 300, status: 'completed' },
];

// Вычислить:
// - totalRevenue (только completed)
// - averageOrder
// - completedCount
```

**Подсказки:**
- Используйте `reduce()` для суммирования
- Фильтруйте по статусу перед вычислением
- Можно вычислить все метрики в одном проходе

---

## Задача 1.9: Получение вложенного значения по пути 🟡

**Контекст:** Нужно безопасно получить значение из вложенного объекта (аналог lodash.get).

**Задача:**
```typescript
const user = {
  id: 1,
  profile: {
    name: 'John',
    address: {
      city: 'New York',
      street: '5th Avenue'
    }
  }
};

// Функция: get(object, path, defaultValue)
// get(user, 'profile.address.city') → 'New York'
// get(user, 'profile.phone.number') → undefined
// get(user, 'profile.phone.number', 'N/A') → 'N/A'
```

**Подсказки:**
- Разделите путь по точке
- Проходите по объекту пошагово
- Возвращайте default, если путь не существует
- Обрабатывайте null/undefined на промежуточных уровнях

---

## Задача 1.10: Flatten массива с вложенными категориями 🟡

**Контекст:** У вас древовидная структура категорий, нужно получить плоский список для dropdown.

**Задача:**
```typescript
const categories = [
  {
    id: 1,
    name: 'Electronics',
    children: [
      { id: 2, name: 'Laptops', children: [] },
      { id: 3, name: 'Phones', children: [
        { id: 4, name: 'iOS', children: [] },
        { id: 5, name: 'Android', children: [] },
      ]},
    ]
  },
  {
    id: 6,
    name: 'Clothing',
    children: []
  }
];

// Получить плоский массив: [{ id: 1, name: 'Electronics' }, { id: 2, name: 'Laptops' }, ...]
```

**Подсказки:**
- Используйте рекурсию или итерацию со стеком
- Можно сохранить уровень вложенности для отступов в UI
- `flatMap()` может помочь

---

## Задача 1.11: Сортировка массива с учетом приоритетов 🟡

**Контекст:** Нужно отсортировать список задач: сначала по приоритету, потом по дате.

**Задача:**
```typescript
const tasks = [
  { id: 1, title: 'Fix bug', priority: 'high', date: '2024-01-15' },
  { id: 2, title: 'Review PR', priority: 'low', date: '2024-01-10' },
  { id: 3, title: 'Deploy', priority: 'high', date: '2024-01-10' },
  { id: 4, title: 'Meeting', priority: 'medium', date: '2024-01-12' },
];

// priority order: high > medium > low
// При одинаковом priority сортировать по date (раньше → первее)
```

**Подсказки:**
- Создайте mapping для priority (high: 3, medium: 2, low: 1)
- В функции сравнения сравнивайте сначала priority, потом date
- Не забудьте создать копию массива перед sort()

---

## Задача 1.12: Пагинация массива 🟢

**Контекст:** Реализовать клиентскую пагинацию для списка элементов.

**Задача:**
```typescript
const items = Array.from({ length: 100 }, (_, i) => ({ id: i + 1, name: `Item ${i + 1}` }));

// Функция: paginate(array, page, pageSize)
// paginate(items, 1, 10) → items 1-10
// paginate(items, 2, 10) → items 11-20
```

**Подсказки:**
- Используйте `slice()`
- Вычислите start и end индексы
- Верните также метаданные: totalPages, hasNext, hasPrev
