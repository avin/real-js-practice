# Работа с формами и валидация

## Задача 2.1: Валидация email 🟢

**Контекст:** Нужно проверять email при вводе в форму регистрации.

**Задача:**
```typescript
// Функция: validateEmail(email: string): boolean
// Должна проверять базовую корректность email
```

**Тест-кейсы:**
- `test@example.com` → true
- `test@example` → false
- `test.name+tag@example.co.uk` → true
- `@example.com` → false
- `test@@example.com` → false

<details>
<summary>💡 Подсказки</summary>

- Используйте регулярное выражение
- Не обязательно покрывать все edge cases RFC, базовая валидация достаточна
- Учитывайте точки и плюсы в email

</details>
---

## Задача 2.2: Валидация пароля 🟢

**Контекст:** Пароль должен соответствовать требованиям безопасности.

**Задача:**
```typescript
// Функция: validatePassword(password: string)
// Возвращает объект: { valid: boolean, errors: string[] }

// Требования:
// - Минимум 8 символов
// - Хотя бы одна заглавная буква
// - Хотя бы одна цифра
// - Хотя бы один спецсимвол (!@#$%^&*)
```

<details>
<summary>💡 Подсказки</summary>

- Проверяйте каждое требование отдельно
- Собирайте все ошибки в массив
- Используйте регулярные выражения для каждой проверки

</details>
---

## Задача 2.3: Debounce для автопоиска 🟡

**Контекст:** При вводе в поисковую строку не нужно делать запрос на каждый символ.

**Задача:**
```typescript
// Создать функцию debounce, которая откладывает выполнение
// Используется так:
const searchAPI = (query: string) => {
  console.log('Searching for:', query);
  // fetch API call
};

const debouncedSearch = debounce(searchAPI, 300);

// При вводе "hello":
// h - запуск таймера
// he - сброс таймера, новый запуск
// hel - сброс таймера, новый запуск
// ... через 300ms после последнего ввода - вызов функции
```

<details>
<summary>💡 Подсказки</summary>

- Используйте `setTimeout` и `clearTimeout`
- Храните ссылку на таймер в замыкании
- Учитывайте передачу аргументов и контекста (this)

</details>
---

## Задача 2.4: Автосохранение формы 🟡

**Контекст:** Форма должна автоматически сохраняться в localStorage при изменениях.

**Задача:**
```typescript
// Создать класс FormAutoSave
class FormAutoSave {
  constructor(formId: string, saveDelay: number = 1000) {}

  // При изменении любого поля - сохранять через debounce
  // Восстанавливать данные при загрузке
  // Очищать при успешной отправке
}

// Использование:
const autoSave = new FormAutoSave('registration-form');
```

<details>
<summary>💡 Подсказки</summary>

- Используйте debounce из предыдущей задачи
- Слушайте события input/change на форме
- Сохраняйте FormData как JSON в localStorage
- Ключ для хранения можно делать по formId

</details>
---

## Задача 2.5: Валидация телефона с форматированием 🟡

**Контекст:** При вводе телефона автоматически форматировать его.

**Задача:**
```typescript
// Функция: formatPhone(input: string): string
// Должна принимать любой ввод и возвращать отформатированный телефон

// Примеры:
// '1234567890' → '+1 (234) 567-8901'
// '12345' → '+1 (234) 5'
// 'abc123def456' → '+1 (234) 56' (убрать нечисловые символы)
```

<details>
<summary>💡 Подсказки</summary>

- Удалите все нечисловые символы
- Добавляйте форматирование по мере ввода
- Ограничьте длину (например, 11 цифр для US)
- Используйте slice() для разделения на части

</details>
---

## Задача 2.6: Динамическое добавление полей формы 🟢

**Контекст:** Пользователь может добавлять/удалять поля (например, список навыков).

**Задача:**
```typescript
// HTML: кнопка "Добавить навык", список полей
// Нужна функция для:
// 1. Добавления нового поля
// 2. Удаления поля
// 3. Получения всех значений

interface Skill {
  id: string;
  value: string;
}

class DynamicFields {
  addField(): void {}
  removeField(id: string): void {}
  getAllValues(): Skill[] {}
}
```

<details>
<summary>💡 Подсказки</summary>

- Генерируйте уникальный id для каждого поля (Date.now() или счетчик)
- Храните массив полей в состоянии
- При удалении фильтруйте массив
- Рендерите поля динамически

</details>
---

## Задача 2.7: Валидация формы в реальном времени 🟡

**Контекст:** Показывать ошибки под полями по мере их заполнения (не после submit).

**Задача:**
```typescript
interface ValidationRule {
  field: string;
  validate: (value: string) => string | null; // null если валидно, иначе текст ошибки
}

class FormValidator {
  constructor(rules: ValidationRule[]) {}

  validateField(field: string, value: string): string | null {}
  validateAll(formData: Record<string, string>): Record<string, string | null> {}
}

// Пример использования:
const validator = new FormValidator([
  { field: 'email', validate: (v) => validateEmail(v) ? null : 'Invalid email' },
  { field: 'password', validate: (v) => v.length >= 8 ? null : 'Min 8 characters' },
]);
```

<details>
<summary>💡 Подсказки</summary>

- Валидируйте на событии blur (когда поле теряет фокус)
- Можно также валидировать на input для мгновенной обратной связи
- Храните состояние ошибок отдельно от значений полей

</details>
---

## Задача 2.8: Подтверждение пароля 🟢

**Контекст:** Два поля: "пароль" и "подтвердите пароль" должны совпадать.

**Задача:**
```typescript
// Функция: validatePasswordMatch(password: string, confirmPassword: string): boolean
// Также нужно показывать ошибку в UI в реальном времени
```

<details>
<summary>💡 Подсказки</summary>

- Валидируйте при вводе во второе поле
- Также ре-валидируйте второе поле при изменении первого
- Показывайте ошибку только если пользователь начал вводить во второе поле

</details>
---

## Задача 2.9: Checkbox "Выбрать все" 🟢

**Контекст:** Список с чекбоксами и master-чекбокс "Выбрать все/Снять все".

**Задача:**
```typescript
// Создать логику для:
// 1. При клике на "Выбрать все" - отметить все элементы
// 2. При клике на "Снять все" - снять все отметки
// 3. Если все элементы отмечены вручную - "Выбрать все" тоже должен отметиться
// 4. Если хотя бы один элемент не отмечен - "Выбрать все" должен сняться

interface Item {
  id: string;
  checked: boolean;
}

class SelectAllManager {
  items: Item[];

  toggleAll(checked: boolean): void {}
  toggleItem(id: string): void {}
  isAllSelected(): boolean {}
}
```

<details>
<summary>💡 Подсказки</summary>

- Храните состояние каждого чекбокса
- Вычисляйте состояние "Выбрать все" на основе items
- Используйте `every()` для проверки всех отмеченных

</details>
---

## Задача 2.10: Ограничение длины textarea с счетчиком 🟢

**Контекст:** Textarea для описания с ограничением в 200 символов и показом оставшихся.

**Задача:**
```typescript
// Создать функциональность:
// 1. Показывать "150/200" под полем
// 2. Не давать вводить больше максимума
// 3. Менять цвет счетчика на красный при приближении к лимиту (например, осталось < 20)

interface CharCounter {
  maxLength: number;
  currentLength: number;
  remaining: number;
  isNearLimit: boolean; // true если осталось < 20
}
```

<details>
<summary>💡 Подсказки</summary>

- Слушайте событие input
- Обрезайте значение если превышен лимит
- Вычисляйте remaining и обновляйте UI

</details>
---

## Задача 2.11: Валидация файла при загрузке 🟡

**Контекст:** При загрузке файла проверять тип и размер.

**Задача:**
```typescript
interface FileValidation {
  valid: boolean;
  errors: string[];
}

// Функция: validateFile(file: File, options: {
//   maxSize: number; // в байтах
//   allowedTypes: string[]; // ['image/jpeg', 'image/png']
// }): FileValidation

// Проверки:
// - Размер не превышает maxSize
// - Тип файла в списке разрешенных
```

<details>
<summary>💡 Подсказки</summary>

- Используйте file.size и file.type
- Конвертируйте размер в MB для понятных сообщений об ошибках
- Проверяйте на событии change у input[type="file"]

</details>
---

## Задача 2.12: Зависимые поля формы 🟡

**Контекст:** Некоторые поля показываются только при определенных значениях других полей.

**Задача:**
```typescript
// Пример:
// - Поле "Тип доставки" (самовывоз/доставка)
// - Если выбрана "доставка", показать поля адреса
// - Если "самовывоз", показать список пунктов выдачи

interface FormField {
  name: string;
  visible: boolean;
  dependsOn?: {
    field: string;
    value: any;
  };
}

class ConditionalForm {
  fields: FormField[];

  updateVisibility(changedField: string, value: any): void {}
  getVisibleFields(): FormField[] {}
}
```

<details>
<summary>💡 Подсказки</summary>

- Храните конфигурацию зависимостей
- При изменении поля проверяйте, влияет ли оно на видимость других
- Обновляйте visible флаг и ре-рендерите форму

</details>